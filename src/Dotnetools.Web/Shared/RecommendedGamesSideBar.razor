<MCard MaxWidth="450"
       Class="mx-auto">
    <MList ThreeLine>
        <MSubheader>推荐游戏</MSubheader>
        @foreach (var item in _items)
        {
            <MDivider Inset="true"></MDivider>
            <MListItem Class="game-item">
                <MListItemAvatar>
                    <MImage Src="@item.Cover"></MImage>
                </MListItemAvatar>

                <MListItemContent>
                    <MListItemTitle Class="game-title">
                        <a href="@item.Url">@((MarkupString)item.Name)</a>
                    </MListItemTitle>
                    <MListItemSubtitle Class="game-memo">@((MarkupString)item.Memo)</MListItemSubtitle>
                </MListItemContent>
            </MListItem>
        }
    </MList>
</MCard>

@code
{
    readonly GameItem[] _items = new[]
    {
        GameKind.Tetris.GameItem()!,
        GameKind.Minesweeper.GameItem()!
    };
}