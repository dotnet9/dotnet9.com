@using Dotnet9.Contracts.Tags
@inject ICaller Caller
@{
    var tags = await Caller.GetAsync<List<TagBrief>>("/api/tag/hot");
}
<div class="tags">
    <div class="side_title">
        <a href="/">更多</a>
        <h3>热门标签</h3>
    </div>
    @if (tags?.Any() == true)
    {
        <ul>
            @foreach (var tag in tags)
            {
                <li>
                    <a href="/tag/@HtmlEncoder.Encode(tag.Name)">@tag.Name</a>
                </li>
            }
        </ul>
    }
</div>